{
  "name": "dump-http",
  "version": "1.0.0",
  "main": "index.js",
  "description": "dump-http is an HTTP dump server, which can be useful for testing purposes",
  "scripts": {
    "test": "ava --verbose",
    "dev": "DEBUG=dump* ./bin/start",
    "start": "./bin/start",
    "coverage": "nyc npm t"
  },
  "bin": {
    "dump-http": "./bin/cli"
  },
  "author": "Michele Pangrazzi <michele.pangrazzi@paros.srl>",
  "license": "MIT",
  "devDependencies": {
    "ava": "^0.19.1",
    "nyc": "^11.0.3",
    "supertest": "^3.0.0"
  },
  "dependencies": {
    "chalk": "^1.1.3",
    "common-tags": "^1.4.0",
    "debug": "^2.6.8",
    "moment": "^2.18.1",
    "raw-body": "^2.2.0"
  },
  "engines": {
    "node": ">=7.6.0"
  }
}
